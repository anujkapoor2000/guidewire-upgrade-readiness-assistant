# Enable the AI features (upgrade advisory + data migration remediation).
# Without this, the app falls back to deterministic summaries so every screen still works.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the Claude model (defaults to claude-opus-4-8).
# ANTHROPIC_MODEL=claude-opus-4-8
